{
  "name": "vulnerable app",
  "version": "1.0.0",
  "description": "Unsafe `source&sink` Demo",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && cp src/index.html dist/index.html",
    "sinker": "sinker --context-depth=3 ./src",
    "serve": "serve dist",
    "start": "npm run build && npm run serve"
  },
  "devDependencies": {
    "sinker": "file:..",
    "serve": "^14.2.5",
    "typescript": "^5.5.3"
  },
  "private": true
}
